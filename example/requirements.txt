# 워밍업 예제 의존성 (CPU로 충분, GPU 불필요)
# 설치:  pip install -r requirements.txt
# torch를 CPU 전용 경량 휠로 받으려면:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0
matplotlib>=3.5
